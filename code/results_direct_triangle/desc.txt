Results using TriangleSampling with temperature=1.0
Loss = 0.01
            * tf.reduce_mean(
                tf.reduce_sum(
                    tfd.ExpRelaxedOneHotCategorical(temperature=1.0, logits=logits).log_prob(
                        barycentric_coords
                    )
                    - self.prior.log_prob(barycentric_coords),
                    axis=-1,
                )
                + 0.2 * tf.reduce_sum(tf.square(bias))
                + 10 * tf.reduce_sum(tf.square(tf.math.log(scale_factors)), axis=-1)
            )